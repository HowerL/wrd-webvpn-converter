# WRD WebVPN 转换器

一个针对 Chromium 内核浏览器（如 Chrome / Microsoft Edge）的扩展，用于把当前标签页的 URL 转换为 WRD WebVPN 可接受的加密 URL 并跳转。适用于北京网瑞达科技有限公司的资源访问控制网关（WebVPN）的产品。

## 📥 安装

### 从应用商店安装（推荐）

- [Chrome 应用商店](https://chromewebstore.google.com/detail/wrd-webvpn-%E8%BD%AC%E6%8D%A2%E5%99%A8/ghjombjaionldgoakjkgbjfigginmahk)
- [Edge 扩展商店](https://microsoftedge.microsoft.com/addons/detail/gkejpppjheiemamfpffhpfbonkbfbplg)

### 从源码构建

1. **安装依赖：** `npm install`

2. **构建扩展：** `npm run build-extension`

3. **加载未打包的扩展：** 在扩展管理页开启开发者模式，选择 `dist` 目录。

## ✨ 关键功能与亮点

1. 🚀 一键跳转：将当前标签页 URL 快速转换为 WebVPN URL，可立即在当前页跳转，或在新标签页打开
2. 🖱️ 快捷操作：在页面右键菜单中提供“跳转至 WebVPN”入口，鼠标只需点两下
3. ⚙️ 可自定义：支持保存 WebVPN baseURL，下次直接使用无需重复输入
4. 🔒 本地处理：仅生成访问链接，所有转换在本地完成，数据不会被上传到第三方服务器
5. 💾 使用便捷：适合需要通过 WRD WebVPN 访问内部资源的企业、学校与运维人员

## 📂 项目结构

```
wrd-webvpn-converter/
├── extension/               # 扩展源代码
│   ├── _locales/            # 多语言翻译文件
│   │   ├── en/              # 英文
│   │   └── zh_CN/           # 简体中文
│   ├── icons/               # 扩展图标
│   ├── background.js        # 后台脚本（右键菜单）
│   ├── convert.js           # URL 加密逻辑
│   ├── popup.html           # 弹出界面
│   ├── popup.js             # 弹出界面逻辑
│   └── manifest.json        # 扩展清单文件
├── docs/                    # 项目主页
├── build-extension.js       # 构建脚本
└── package.json
```

- `popup.html`, `popup.js`：弹出用户交互界面与逻辑，持久化键为 `baseURL`
- `convert.js`：加密逻辑，AES key 硬编码为 `wrdvpnisthebest!`
- `background.js`：注册页面上下文菜单，点击时读取 `baseURL` 并跳转

## 🛠️ 开发环境

- Node.js >= 20
- 支持 Manifest V3 扩展的 Chromium 内核浏览器（Chrome、Edge 等）
